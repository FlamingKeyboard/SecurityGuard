# Vivint API (already in vivintpy subdirectory, but listing its deps)
aiohttp>=3.8.5
certifi>=2022.9.24
pubnub>=7
grpcio>=1.51.1
protobuf>=4.23.1
pyjwt>=2.9.0

# Gemini API
google-generativeai>=0.3.0

# Windows token encryption (optional but recommended)
pywin32>=306
