# Vivint Security Guard - Environment Variables
# Copy this file to .env and fill in your values
# DO NOT commit .env to git!

# Vivint credentials
VIVINT_USERNAME=your@email.com
VIVINT_PASSWORD=your_vivint_password

# Gemini API key (from https://aistudio.google.com/apikey)
GEMINI_API_KEY=AIza...

# Pushover credentials (from https://pushover.net)
PUSHOVER_TOKEN=your_app_token
PUSHOVER_USER=your_user_key

# Vivint Hub IP - use Tailscale IP for cloud deployment
# Find it with: tailscale status | grep vivint (or check your router's Tailscale IP)
# If using subnet routing, use the local IP: 192.168.8.132
VIVINT_HUB_IP=100.x.x.x

# Optional: RTSP port (default 8554)
# VIVINT_HUB_RTSP_PORT=8554
