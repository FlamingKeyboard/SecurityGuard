# Vivint Security Guard - Environment Variables
# Copy this file to .env and fill in your values
# DO NOT commit .env to git!

# Vivint credentials
VIVINT_USERNAME=your@email.com
VIVINT_PASSWORD=your_vivint_password

# Gemini API key (from https://aistudio.google.com/apikey)
GEMINI_API_KEY=AIza...

# Pushover credentials (from https://pushover.net)
PUSHOVER_TOKEN=your_app_token
PUSHOVER_USER=your_user_key

# Vivint Hub IP - REQUIRED
# Find it by checking your router's DHCP leases or running: nmap -sn 192.168.1.0/24
# For cloud deployment: use your hub's Tailscale IP (tailscale status)
VIVINT_HUB_IP=192.168.1.x

# Optional: RTSP port (default 8554)
# VIVINT_HUB_RTSP_PORT=8554

# =============================================================================
# Google Cloud Platform Integration (Optional)
# =============================================================================
# Enables image archival to GCS and Gemini API logging to BigQuery

# GCP Project ID (required for GCP features)
# GCP_PROJECT_ID=your-project-id

# Service account JSON key file (local development only)
# On GCE, leave empty to use Application Default Credentials (ADC)
# GCP_SERVICE_ACCOUNT_FILE=/path/to/service-account.json

# GCS bucket name (auto-generated as securityguard-{project_id} if empty)
# GCS_BUCKET_NAME=

# BigQuery settings (defaults: security_guard.security_logs)
# BQ_DATASET=security_guard
# BQ_TABLE=security_logs

# Archival settings (days before archiving to GCP)
# IMAGE_RETENTION_DAYS=30
# LOG_RETENTION_DAYS=30

# Disk space threshold (GB) - trigger archival if below this value
# DISK_SPACE_THRESHOLD_GB=10

# Sync interval (seconds) - how often to sync to GCP
# SYNC_INTERVAL_SECONDS=3600

# =============================================================================
# Video Capture (Experimental)
# =============================================================================
# Capture short video clips instead of still frames for better AI context.
# Gemini can analyze motion, direction, and behavior patterns in video.
# Uses remux (no re-encoding) to minimize CPU usage on e2-micro VMs.

# Enable video capture (default: false - uses frame capture)
# VIDEO_CAPTURE_ENABLED=true

# Video clip duration in seconds (default: 3)
# VIDEO_CAPTURE_DURATION=3

# =============================================================================
# Multi-Camera Correlation (Experimental)
# =============================================================================
# When motion is detected, capture video from adjacent cameras simultaneously
# for correlated analysis. Gemini analyzes all views together to track movement
# across the property (e.g., person walking from driveway to doorbell).
# Requires VIDEO_CAPTURE_ENABLED=true

# Enable multi-camera capture (default: false)
# MULTI_CAMERA_ENABLED=true

# Maximum cameras to capture simultaneously (default: 3)
# Set lower on resource-constrained VMs to reduce CPU/memory usage
# MAX_CONCURRENT_CAMERAS=3

# Camera adjacency is configured in config.py CAMERA_ADJACENCY dict
# Update the camera names there to match your Vivint camera names
